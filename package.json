{
  "name": "bnb-turnkey-qc-free",
  "version": "1.0.0",
  "description": "BNB Turnkey Sales Call QC System — Free Edition (SQLite + Gemini Free Tier)",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "migrate": "node migrations/run.js",
    "process": "node -e \"require('./src/workers/qcWorker').processQueue(3).then(r => { console.log(r); process.exit(); })\""
  },
  "dependencies": {
    "express": "^4.21.0",
    "better-sqlite3": "^11.6.0",
    "cors": "^2.8.5",
    "helmet": "^8.0.0",
    "morgan": "^1.10.0",
    "dotenv": "^16.4.0",
    "node-cron": "^3.0.3"
  }
}
